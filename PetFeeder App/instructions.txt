npm install
npm npm audit fix
npm install
npm run dev
npm install --save-dev cross-env


###change in package.json
"scripts": {
  "dev": "cross-env NODE_ENV=development tsx server/index.ts"
}


###if not .env:
npm install dotenv
create .env file in root 
example content : 
DATABASE_URL='postgres://postgres:postgre@localhost:5432/postgres'
# format = postgres://<user>:<password>@<host>:<port>/<database>

update files for .env:
import dotenv from 'dotenv';
dotenv.config();


### For using postgre
npm install pg drizzle-orm
and remove the repelit server db
npm uninstall @neondatabase/serverless drizzle-orm/neon-serverless


# port related issues sometimes:
check if server/index.ts:
host: "127.0.0.1",  //localhost or 127.0.0.1
and const port = 5001; // or depending upon usage
also sometime the existing dosent work so try using the below snippet:
server.listen(port, () => {
  log(`serving on port ${port}`);
});

###if ❌ Error 1: relation "users" does not exist
npx drizzle-kit push // to run migrations
### if that didn't work then try:
npx drizzle-kit generate
npx drizzle-kit push